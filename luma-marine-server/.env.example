PORT=3000
JWT_SECRET=replace-with-a-long-random-string
CORS_ORIGIN=*
DATA_DIR=./data-store

# Optional: pin the bootstrap admin account instead of getting a random
# password printed to the console on first run.
# ADMIN_BOOTSTRAP_EMAIL=admin@lumamarine.se
# ADMIN_BOOTSTRAP_PASSWORD=change-me
