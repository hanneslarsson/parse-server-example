PORT=3000
JWT_SECRET=replace-with-a-long-random-string
CORS_ORIGIN=*
DATA_DIR=./data-store

# Optional: pin the bootstrap admin accounts instead of getting a random
# password printed to the console on first run. Never commit real values —
# put them in your local .env only (gitignored).
# ADMIN_BOOTSTRAP_EMAIL=admin@lumamarine.se
# ADMIN_BOOTSTRAP_PASSWORD=change-me
# MATTIAS_PASSWORD=change-me
# HANNES_PASSWORD=change-me
